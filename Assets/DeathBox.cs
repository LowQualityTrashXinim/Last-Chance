using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeathBox : MainTile
{
    // Start is called before the first frame update
    public override void SetDefault()
    {
    }
    // Update is called once per frame
}
